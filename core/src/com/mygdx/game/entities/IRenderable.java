package com.mygdx.game.entities;
/**
 * IRenderable is an interface implemented by classes that need to render objects
 * in the game.
 */
public interface IRenderable {
    void render(float deltaTime);
}
